import configStyles from "./config-styles";

export default {
  root: {
    width: 500,
    height: 500,
    display:"grid",
    gridTemplateColumns:"repeat(3, 1fr)",
    gridTemplateRows:"repeat(3, 1fr)",
    gridGap: 10,
    margin: "50px auto"
  }
}